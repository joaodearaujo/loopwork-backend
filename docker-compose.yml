services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: loopwork
      POSTGRES_PASSWORD: senha
    ports:
      - "5432:5432"
    volumes:
      - loopwork-data:/var/lib/postgresql/data

volumes:
  loopwork-data: