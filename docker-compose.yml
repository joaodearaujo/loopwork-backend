services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: loopwork
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - loopwork-data:/var/lib/postgresql/data

volumes:
  loopwork-data: